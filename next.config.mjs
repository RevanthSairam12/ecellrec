/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["acertinity ui.com", "images.unsplash.com", "assets.aceternity.com"]
    },
};

export default nextConfig;
